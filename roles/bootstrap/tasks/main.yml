---
# main tasks file for bootstrap
- name: Install packages
  ansible.builtin.include_tasks:
    file: pkg.yml

- name: Set up shell
  ansible.builtin.include_tasks:
    file: shell.yml

- name: Set up Emacs
  ansible.builtin.include_tasks:
    file: emacs.yml
